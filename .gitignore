# If you need to exclude files such as those generated by an IDE, use
# $GIT_DIR/info/exclude or the core.excludesFile configuration variable as
# described in https://git-scm.com/docs/gitignore

*.egg-info
*.pot
*.py[co]
.tox/
__pycache__
MANIFEST
dist/
docs/_build/
docs/locale/
node_modules/
tests/coverage_html/
tests/.coverage
build/
tests/report/

# This only ignore the static directory at the top-most level.
# This directory is used to serve the static files during production.
# The command "python manage.py collectstatic" must be called when deploying.
# This command is not necessary during development. This directory is ignored
# because tracking this files is meaningless since they are in the static
# directories inside each individual app.
static/

# For python decouple.
.env
media/
db.sqlite3